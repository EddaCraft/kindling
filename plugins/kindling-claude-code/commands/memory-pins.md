---
description: List all pinned observations with their notes.
---

List all pinned observations with their notes and content preview.

Run this command:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/memory-pins.js"
```

Show the results to the user.
